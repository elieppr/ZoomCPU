name: basic-demo
description:
tags:
initialize:
  outputs: ['yaml']
  plugins:
    teads-curve: 
      path: '@grnsft/if-unofficial-plugins'
      method: TeadsCurve
      global-config:
        interpolation: spline
    simple-html-exporter:
      method: SimpleHtmlExporter
      path: "@grnsft/if-plugins"
tree:
  children:
    child-0:
      defaults:
        cpu/thermal-design-power: 100
        grid/carbon-intensity: 1
      pipeline:
        - teads-curve
        - simple-html-exporter
      config:
        simple-html-exporter:
          html-path: /Users/eliana/Documents/GitHub/ZoomCPU/html.html
      inputs:
