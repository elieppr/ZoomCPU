import { GlobalPlugins } from '../types/manifest';
import { PluginStorageInterface } from '../types/plugin-storage';
/**
 * Registers all plugins from `manifest`.`initalize` property.
 */
export declare const initalize: (plugins: GlobalPlugins) => Promise<PluginStorageInterface>;
