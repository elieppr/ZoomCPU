import { ResourceSku, VirtualMachine } from '@azure/arm-compute';
import { GetMetricsParams } from './types';
export declare class AzureAPI {
    private credential;
    private client;
    constructor();
    /**
     * Fetches metrics for a specific virtual machine.
     */
    getMetricsTimeseries(params: GetMetricsParams, metricName: string): Promise<import("@azure/arm-monitor").MetricValue[]>;
    /**
     * Sets up the ComputeManagementClient with the specified subscription ID.
     */
    private setClient;
    /**
     * Fetches a list of virtual machines within a specific resource group.
     */
    getVMDataByResourceGroupName(resourceGroupName: string): Promise<VirtualMachine[]>;
    /**
     * Fetches a list of available resource Skus.
     */
    getResourceSkus(): Promise<ResourceSku[]>;
}
