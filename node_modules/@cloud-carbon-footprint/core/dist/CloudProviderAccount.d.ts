import { EstimationResult, GroupBy } from '@cloud-carbon-footprint/common';
import { Region } from '.';
export default class CloudProviderAccount {
    id?: string;
    name?: string;
    getRegionData(cloudProvider: string, region: Region, startDate: Date, endDate: Date, grouping: GroupBy): Promise<EstimationResult[]>;
}
