import { StorageUsage, HDDStorageService, Cost } from '@cloud-carbon-footprint/core';
import { ServiceWrapper } from './ServiceWrapper';
export default class S3 extends HDDStorageService {
    private readonly serviceWrapper;
    serviceName: string;
    constructor(serviceWrapper: ServiceWrapper);
    getUsage(startDate: Date, endDate: Date): Promise<StorageUsage[]>;
    getCosts(start: Date, end: Date, region: string): Promise<Cost[]>;
}
